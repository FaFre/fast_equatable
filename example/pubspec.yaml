name: example
description: A simple command-line application.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  equatable: ^2.0.3
  fast_equatable:
    path: ../

dev_dependencies:
  lints: ^2.0.0
